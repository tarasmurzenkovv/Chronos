import {createStyles} from '@material-ui/core';

const styles = () =>
  createStyles({
    button: {
      color: '#ffffff',
      position: 'fixed',
      bottom: 0,
      right: 0,
      transform: 'translate(-25%, -25%)'
    }
  });

export default styles;
